local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local HttpService = game:GetService("HttpService")

-- ==========================================
-- ⚙️ PENGATURAN KEY SYSTEM (GANTI DI SINI)
-- ==========================================
local CorrectKey = "MasAripGanteng" -- Ganti dengan password/key yang kamu inginkan
local GetKeyURL = "https://discord.com/users/409302496163659776" -- Link untuk user mendapatkan key (Discord/Linkvertise)
local MainScriptURL = "https://raw.githubusercontent.com/aspago-lua/rbx-script/refs/heads/main/iniMAIN-MALS-letter"
local KeyFileName = "MALS_SavedKey.txt"
-- ==========================================

local cloneref = cloneref or function(o) return o end
local gethui = gethui or function() return CoreGui end
local targetParent = pcall(gethui) and gethui() or CoreGui

local THEME = {
    Background = Color3.fromRGB(30, 30, 30),
    ItemBG = Color3.fromRGB(45, 45, 45),
    Accent = Color3.fromRGB(10, 132, 255),
    Text = Color3.fromRGB(255, 255, 255),
    SubText = Color3.fromRGB(152, 152, 157),
    Success = Color3.fromRGB(50, 215, 75),
    Warning = Color3.fromRGB(255, 69, 58),
    Stroke = Color3.fromRGB(255, 255, 255)
}

local function ApplyGlassmorphism(obj, radius, strokeAlpha)
    if obj:IsA("GuiObject") then
        local corner = Instance.new("UICorner", obj)
        corner.CornerRadius = UDim.new(0, radius or 8)
        local str = Instance.new("UIStroke", obj)
        str.Color = THEME.Stroke
        str.Transparency = strokeAlpha or 0.92
        str.Thickness = 1
    end
end

local function Tween(obj, props, time)
    TweenService:Create(obj, TweenInfo.new(time or 0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), props):Play()
end

local function LoadMainScript()
    task.spawn(function()
        loadstring(game:HttpGet(MainScriptURL))()
    end)
end

-- AUTO LOAD KEY
if isfile and isfile(KeyFileName) then
    local savedKey = readfile(KeyFileName)
    if savedKey == CorrectKey then
        LoadMainScript()
        return -- Berhenti di sini, tidak perlu memunculkan UI Key System
    end
end

-- BIKIN UI KEY SYSTEM
local KeyGui = Instance.new("ScreenGui")
KeyGui.Name = "MALS_KeySystem"
KeyGui.Parent = targetParent
KeyGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

local MainFrame = Instance.new("Frame", KeyGui)
MainFrame.Size = UDim2.new(0, 350, 0, 220)
MainFrame.Position = UDim2.new(0.5, -175, 0.5, -110)
MainFrame.BackgroundColor3 = THEME.Background
MainFrame.BackgroundTransparency = 0.05
MainFrame.BorderSizePixel = 0
MainFrame.ClipsDescendants = true
ApplyGlassmorphism(MainFrame, 12, 0.8)

-- Header macOS Style
local Header = Instance.new("Frame", MainFrame)
Header.Size = UDim2.new(1, 0, 0, 40)
Header.BackgroundTransparency = 1

local ControlContainer = Instance.new("Frame", Header)
ControlContainer.Size = UDim2.new(0, 60, 1, 0)
ControlContainer.Position = UDim2.new(0, 15, 0, 0)
ControlContainer.BackgroundTransparency = 1

local RedBtn = Instance.new("TextButton", ControlContainer)
RedBtn.Text = ""
RedBtn.Size = UDim2.new(0, 13, 0, 13)
RedBtn.Position = UDim2.new(0, 0, 0.5, -6.5)
RedBtn.BackgroundColor3 = Color3.fromRGB(255, 95, 86)
Instance.new("UICorner", RedBtn).CornerRadius = UDim.new(1, 0)
RedBtn.MouseButton1Click:Connect(function() KeyGui:Destroy() end)

local YellowBtn = Instance.new("TextButton", ControlContainer)
YellowBtn.Text = ""; YellowBtn.Size = UDim2.new(0, 13, 0, 13); YellowBtn.Position = UDim2.new(0, 22, 0.5, -6.5)
YellowBtn.BackgroundColor3 = Color3.fromRGB(255, 189, 46); Instance.new("UICorner", YellowBtn).CornerRadius = UDim.new(1, 0)

local GreenBtn = Instance.new("TextButton", ControlContainer)
GreenBtn.Text = ""; GreenBtn.Size = UDim2.new(0, 13, 0, 13); GreenBtn.Position = UDim2.new(0, 44, 0.5, -6.5)
GreenBtn.BackgroundColor3 = Color3.fromRGB(39, 201, 63); Instance.new("UICorner", GreenBtn).CornerRadius = UDim.new(1, 0)

local Title = Instance.new("TextLabel", Header)
Title.Text = "MALS Hub - Gateway"
Title.Font = Enum.Font.GothamMedium
Title.TextSize = 14
Title.TextColor3 = THEME.Text
Title.Size = UDim2.new(1, 0, 1, 0)
Title.BackgroundTransparency = 1
Title.TextXAlignment = Enum.TextXAlignment.Center

-- Konten Bawah
local Desc = Instance.new("TextLabel", MainFrame)
Desc.Text = "Please enter your access key to continue."
Desc.Font = Enum.Font.Gotham
Desc.TextSize = 12
Desc.TextColor3 = THEME.SubText
Desc.Size = UDim2.new(1, 0, 0, 20)
Desc.Position = UDim2.new(0, 0, 0, 55)
Desc.BackgroundTransparency = 1
Desc.TextXAlignment = Enum.TextXAlignment.Center

local KeyInputBox = Instance.new("TextBox", MainFrame)
KeyInputBox.Size = UDim2.new(1, -60, 0, 35)
KeyInputBox.Position = UDim2.new(0, 30, 0, 85)
KeyInputBox.BackgroundColor3 = THEME.ItemBG
KeyInputBox.BackgroundTransparency = 0.15
KeyInputBox.Font = Enum.Font.Gotham
KeyInputBox.TextSize = 13
KeyInputBox.TextColor3 = THEME.Text
KeyInputBox.PlaceholderText = "Enter Key Here..."
KeyInputBox.PlaceholderColor3 = THEME.SubText
KeyInputBox.Text = ""
KeyInputBox.ClearTextOnFocus = false
ApplyGlassmorphism(KeyInputBox, 8, 0.9)

local StatusMsg = Instance.new("TextLabel", MainFrame)
StatusMsg.Text = ""
StatusMsg.Font = Enum.Font.GothamMedium
StatusMsg.TextSize = 11
StatusMsg.TextColor3 = THEME.Warning
StatusMsg.Size = UDim2.new(1, 0, 0, 20)
StatusMsg.Position = UDim2.new(0, 0, 0, 125)
StatusMsg.BackgroundTransparency = 1
StatusMsg.TextXAlignment = Enum.TextXAlignment.Center

local CheckBtn = Instance.new("TextButton", MainFrame)
CheckBtn.Text = "Verify Key"
CheckBtn.Font = Enum.Font.GothamMedium
CheckBtn.TextSize = 13
CheckBtn.TextColor3 = THEME.Text
CheckBtn.BackgroundColor3 = THEME.Success
CheckBtn.Size = UDim2.new(0.5, -40, 0, 35)
CheckBtn.Position = UDim2.new(0.5, 10, 0, 155)
ApplyGlassmorphism(CheckBtn, 8, 0.9)

local GetKeyBtn = Instance.new("TextButton", MainFrame)
GetKeyBtn.Text = "Get Key"
GetKeyBtn.Font = Enum.Font.GothamMedium
GetKeyBtn.TextSize = 13
GetKeyBtn.TextColor3 = THEME.Text
GetKeyBtn.BackgroundColor3 = THEME.Accent
GetKeyBtn.Size = UDim2.new(0.5, -40, 0, 35)
GetKeyBtn.Position = UDim2.new(0, 30, 0, 155)
ApplyGlassmorphism(GetKeyBtn, 8, 0.9)

-- Logika Tombol
GetKeyBtn.MouseButton1Click:Connect(function()
    if setclipboard then
        setclipboard(GetKeyURL)
        StatusMsg.Text = "Link copied to clipboard!"
        StatusMsg.TextColor3 = THEME.Success
    else
        StatusMsg.Text = "Executor does not support clipboard."
        StatusMsg.TextColor3 = THEME.Warning
    end
end)

CheckBtn.MouseButton1Click:Connect(function()
    local input = KeyInputBox.Text
    if input == CorrectKey then
        StatusMsg.Text = "Key Verified! Loading..."
        StatusMsg.TextColor3 = THEME.Success
        Tween(CheckBtn, {BackgroundColor3 = Color3.fromRGB(40, 180, 60)}, 0.2)
        
        if writefile then
            pcall(function() writefile(KeyFileName, input) end)
        end
        
        task.wait(0.8)
        KeyGui:Destroy()
        LoadMainScript()
    else
        StatusMsg.Text = "Invalid Key!"
        StatusMsg.TextColor3 = THEME.Warning
        Tween(KeyInputBox, {Position = UDim2.new(0, 25, 0, 85)}, 0.05)
        task.wait(0.05)
        Tween(KeyInputBox, {Position = UDim2.new(0, 35, 0, 85)}, 0.05)
        task.wait(0.05)
        Tween(KeyInputBox, {Position = UDim2.new(0, 30, 0, 85)}, 0.05)
    end
end)

-- Bikin form bisa di-drag
local dragging, dragInput, dragStart, startPos
MainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then dragging = false end
        end)
    end
end)
MainFrame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then dragInput = input end
end)
game:GetService("UserInputService").InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)
